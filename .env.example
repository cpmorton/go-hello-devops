# Environment variables for go-hello-devops
# Copy this file to .env and fill in your values
#
# DO NOT commit .env to git - it contains secrets!
# This .env.example file shows what variables are needed.

# Your GitHub username (for pulling the devops-coderbox image)
GITHUB_USERNAME=yourusername

# Git configuration for commits
# Use your actual name and email
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=your.email@example.com

# Anthropic Claude Code
# Option 1: Get API key from https://console.anthropic.com
# - do not use if you have a monthly pro or max subscription:
#ANTHROPIC_API_KEY=sk-ant-your-key-here
# Option 2: Set these to mount a claude config with auth tokens into the container:
CLAUDE_JSON="~/.claude.json"
CLAUDE_JSON_CONTAINERPATH="/home/coder/.claude.json"

#TODO: generate password during build:
# $(openssl rand -base64 29 | tr -d "=+/" | cut -c1-14)
# Password for the code-server IDE
# Defaults to 'devops-coderbox' if not set
# Change this if exposing the IDE to the internet
CODE_SERVER_PASSWORD=devops-coderbox
